/**
 * Spacing scale — a 4pt base grid. Named t-shirt sizes stay for ergonomics;
 * the gaps (xxs, smd, mlg) fill the most common in-between rhythm needs.
 */
export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  smd: 12,
  md: 16,
  mlg: 20,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
} as const;
