server:
  port: 8082

spring:
  application:
    name: expense-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ecospend}
    username: ${DB_USER:ecospend}
    password: ${DB_PASSWORD:ecospend_dev}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
